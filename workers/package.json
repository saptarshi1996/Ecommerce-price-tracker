{
  "name": "workers",
  "version": "1.0.0",
  "description": "Ecommerce price tracker workers",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "npm run start",
    "lint": "eslint . --ext .ts",
    "dev": "npx nodemon src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}